# タスク完了時のチェックリスト

タスクを完了する際は、以下を実行してください：

## 1. リンティング
```bash
uv run ruff check .
```
- エラーがあれば修正する（`--fix`オプションで自動修正可能）

## 2. フォーマット
```bash
uv run ruff format .
```

## 3. 動作確認
- 変更が適切に動作することを確認
- 必要に応じて `uv run python main.py` で実行テスト

## 4. Git
- 変更をコミット（ユーザーから指示があれば）
